amqp==5.3.1
asgiref==3.11.1
ast_serialize==0.6.0
billiard==4.2.4
celery==5.6.3
certifi==2026.6.17
cfgv==3.5.0
charset-normalizer==3.4.8
click==8.4.2
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
coverage==7.15.2
crispy-bootstrap5==2026.3
distlib==0.4.3
Django==6.0.5
django-countries==9.0.0
django-crispy-forms==2.6
# Pinned to a commit, not a release. PR #11 (form-builder-api.js extraction)
# has since merged upstream at opensensor/django-forms-workflows@a023570 —
# but as a SQUASH merge, not a merge commit: our old pin (564473a) is NOT an
# ancestor of a023570. The "PRs here merge via a real merge commit, so the
# branch-tip SHA survives merge unchanged" assumption this pin previously
# relied on is confirmed WRONG as of #11; don't rely on it again. The squash
# also picked up fixes for two of the three Copilot XSS findings flagged
# against #11 (`category`/`usage_count` now escaped in form-builder-api.js);
# `template.id` and `error.message` interpolation are still unescaped.
#
# Update: PR #12 merged upstream at opensensor/django-forms-workflows@524cef6
# — via a REAL merge commit this time, not a squash. Confirmed via
# `git merge-base --is-ancestor 5c6deb5 upstream/main`: our old pin (5c6deb5,
# PR #12's fork-branch tip) IS an ancestor of upstream/main today, so the
# pin itself never went stale/invalid — the "squash breaks the SHA"
# assumption from #11 was never universal, just true for that one PR. Still
# treat it as case-by-case per pin, not assumed either way, next time.
#
# Now pinned to the tip of PR #16 (opensensor/django-forms-workflows#16,
# "Extract Canvas/Drag-Drop Controller into separate module") on the
# ViolanteCodes fork it was opened from — not yet merged/approved upstream.
# Same explicit-call caveat as before: do NOT assume this SHA survives #16's
# eventual merge unchanged; re-diff and re-pin once it merges. Overrides
# rebuilt against this pin:
#   - form-builder.js: down to its slim ~130-line orchestrator (constructor,
#     store proxies, init(), setupEventListeners()) now that canvas/drag-drop
#     moved out too. Same 1-hunk Cancel-button-redirect customization
#     carried over unchanged (the line it patches wasn't touched by #16).
#   - form-builder-canvas.js: new upstream file (field palette, canvas/
#     drag-drop, field CRUD, multi-step canvas, context menu) — no override
#     needed, resolves straight to the installed package, same as
#     form-builder-store.js/form-builder-history.js.
#   - form-builder-property-editor.js: picked up an unrelated, already-
#     merged upstream fix (PR #13, "Fix duplicate data-index values in
#     property editor add/save handlers") between our old pin and this one.
#     The existing 2-hunk customization (previousFieldName capture +
#     formSteps-rename-sync, and the multi-step-aware re-render branch in
#     saveFieldProperties) still applies cleanly at its same logical spot,
#     just shifted a few lines by #13's fix — rebuilt fresh against the new
#     pin rather than patched forward, verified byte-for-byte identical in
#     diff shape to the pre-#16 override.
#   - form-builder-api.js: untouched between the old and new pin — same
#     3-hunk customization, unchanged. Still-open, not-ours-to-fix XSS
#     findings from #11 (`template.id`/`error.message` unescaped) confirmed
#     still present at this pin too.
django-forms-workflows @ git+https://github.com/ViolanteCodes/django-forms-workflows.git@150caaa18ba5f492a33949c16181149437ababcb
django-guardian==3.3.2
django-localflavor==5.0
django-nested-admin==4.1.6
django-stubs==6.0.7
django-stubs-ext==6.0.7
django-treebeard==5.3.0
factory_boy==3.3.3
Faker==40.28.1
filelock==3.29.7
identify==2.6.19
idna==3.18
kombu==5.6.2
librt==0.13.0
mypy==2.3.0
mypy_extensions==1.1.0
nodeenv==1.10.0
packaging==26.2
parameterized==0.9.0
pathspec==1.1.1
platformdirs==4.10.0
pre_commit==4.6.0
prompt_toolkit==3.0.52
python-dateutil==2.9.0.post0
python-decouple==3.8
python-discovery==1.4.4
python-monkey-business==1.1.0
python-stdnum==2.2
PyYAML==6.0.3
requests==2.34.2
ruff==0.15.21
six==1.17.0
sqlparse==0.5.5
types-PyYAML==6.0.12.20260518
typing_extensions==4.16.0
tzdata==2026.2
tzlocal==5.4.4
urllib3==2.7.0
vine==5.1.0
virtualenv==21.6.1
wcwidth==0.8.2
